Data in k8s is transient by default.

We can attach a volume to the pod. The data generated by the pod is now stored in the volume. The data remains on the host/node, even after the pod is deleted.
Check random-number-pod.yaml

Different volume options:
