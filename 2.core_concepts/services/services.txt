Services help us to connect application together or applications to users.
Services enable connectivity between pods.
For instance backend and frontend pods.

---

NodePort service: Pod becomes accessible on the port on the node.
ClusterIP: Cluster creates a virtual IP inside the cluster to enable communication between different services.
Loadbalancer: Distribute load accross the services